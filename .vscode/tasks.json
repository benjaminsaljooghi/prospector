{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build blast_demo_app",
            "type": "shell",
            "command": "make",
            "options": {
                "cwd": "${workspaceRoot}/blast"
            },
        },
        {
            "label": "build invoker",
            "type": "shell",
            "command": "make",
            "options": {
                "cwd": "${workspaceRoot}/invoker"
            }
        }
    ]
}